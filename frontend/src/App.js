import React, { Suspense } from "react";
import { Route, Navigate, Routes, useNavigate } from "react-router-dom";

const App = () => {
  return (
    <div>
      <h2>Project 3</h2>
    </div>
  );
};

export default App;
